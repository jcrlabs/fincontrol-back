# Database
DATABASE_URL=postgres://fincontrol:password@localhost:5432/fincontrol?sslmode=disable

# JWT — generate with: make keys
JWT_PRIVATE_KEY_B64=<base64-encoded PEM RSA private key>

# Server
PORT=8080

# CORS (dev only — prod uses defaults fin.jcrlabs.net)
CORS_ORIGIN_DEV=http://localhost:5173
