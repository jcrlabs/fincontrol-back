version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - gosec
    - noctx
  settings:
    gosec:
      excludes:
        - G115

formatters:
  enable:
    - gofmt
